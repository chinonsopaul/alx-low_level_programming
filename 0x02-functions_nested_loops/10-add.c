#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 * @a: first parameter
 * @c: second parameter
 * Return: alway 0
 */
int add(int a, int c)
{
	return (a + c);
}
