#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isalpha - main function
 * @c: parameter
 * Return: always 0
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
