#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - main function
 * @c: parameter
 * Return: always 0
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
