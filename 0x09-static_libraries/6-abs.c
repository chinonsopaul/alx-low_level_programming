#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _abs - main function
 * @n: parameter
 * Return: always 0
 */
int _abs(int n)
{
	return (abs(n));
}
